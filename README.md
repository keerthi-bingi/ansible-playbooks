# ansible-playbooks
This repo is created for storing playbooks !!

# Commands for running ansible-playbooks
- ansible-playbook -i <inventory.ini> <01-playbook.yaml> -e ansible_user=ec2-user -e  ansible_password=DevOps321 -b